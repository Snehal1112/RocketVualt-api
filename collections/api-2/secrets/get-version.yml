uid: 0a42d9be-9b0d-44a1-af1a-9503c424812a
info:
  name: Get Secret Version
  type: http
  seq: 10
http:
  method: GET
  url: '{{base_url}}/api/v1/vaults/{{vault_name}}/secrets/{{secret_id}}/versions/{{version}}'
  auth:
    type: bearer
    token: '{{token}}'
