uid: 98aaa6c8-8c08-4333-af00-585cae01526f
info:
  name: Create Certificate (CA-Signed)
  type: http
  seq: 4
http:
  method: POST
  url: '{{base_url}}/api/v1/vaults/{{vault_name}}/certificates'
  body:
    type: json
    data: |-
      {
        "name": "my-ca-signed-cert",
        "key_id": "{{key_id}}",
        "ca_cert_id": "{{ca_cert_id}}",
        "validity_days": 365,
        "auto_renew": true,
        "renewal_days": 30,
        "tags": ["env:dev"]
      }
  auth:
    type: bearer
    token: '{{token}}'
