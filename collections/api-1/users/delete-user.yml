uid: 954c25d1-064d-40c4-a674-3a5630cd6a4b
info:
  name: Delete User
  type: http
http:
  method: DELETE
  url: '{{base_url}}/api/v1/users/{{user_id}}'
  auth:
    type: bearer
    token: '{{token}}'
runtime:
  scripts:
  - type: tests
    code: |2-

      test("Body is not empty", () => {
        expect(res.getBody()).to.not.be.empty;
      });
settings:
  encodeUrl: true
  timeout: 0.0
  followRedirects: true
  maxRedirects: 5.0
  verifySsl: true
