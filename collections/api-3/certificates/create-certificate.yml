uid: ca29e52b-6540-41a1-b015-3d82ba0fb6ef
info:
  name: Create Certificate
  type: http
  seq: 3
http:
  method: POST
  url: '{{base_url}}/api/v1/vaults/{{vault_name}}/certificates'
  body:
    type: json
    data: |-
      {
        "name": "my-cert",
        "key_id": "{{key_id}}",
        "validity_days": 365,
        "auto_renew": true,
        "renewal_days": 30,
        "tags": ["env:dev"]
      }
  auth:
    type: bearer
    token: '{{token}}'
